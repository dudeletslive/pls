extends ../../layouts/default

block content
	div.myAccount
		section.spacer
		div.jumbotron: .container
			div.panel
				form(method="post")#myAccount
					input(type='hidden', name='action', value='confirm.details')
					h1.lead MPDX Would Like Permission to Access Your Account
				form(method="post")
					//- input(type='text', value='#{redirectURI}/?code=#{code}&?state=#{state}')
					label Redirect URI
					input(name="redirectURI", value="#{redirectURI}")
					label State
					input(name="state", value="#{state}")
					label MPDX code
					input(name="MPDX Code", value="#{code}")
					button(type='submit') Allow
					//- p= redirectURI
					//- p= code
					+flash-messages(messages)
					
		section.spacer